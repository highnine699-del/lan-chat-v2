# Core server dependencies
flask==3.1.3
flask-socketio==5.6.1
python-socketio==5.16.1

# Control Center (launcher) — optional but recommended
psutil>=5.9

# Test dependencies (optional — only needed to run the test suite)
# pip install pytest
